<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
</head>
<style>
    .custom-card {
        width: 20rem; /* Ajuste a largura do card */
        height: 25rem; /* Ajuste a altura do card */
    }
    .custom-card .card-img-top {
        height: 15rem; /* Ajuste a altura da imagem */
        object-fit: cover; /* Faz com que a imagem se ajuste ao tamanho sem distorcer */
    }
    .custom-card .card-body {
        height: 20rem; /* Ajuste a altura do corpo do card */
        overflow: hidden; /* Esconde o conteúdo que ultrapassa a altura definida */
    }
</style>
<body class="container mt-5">
    <div class="card custom-card">
        <h3 class="card-header"> Pokemon</h3>
        <div class="card-text ">
            <div class="">
                <img clas="img-fluid"src="<%= pokemon.sprites.front_default %>" alt="<%= pokemon.name %>">
                <h2> Nome: <%= pokemon.name %> </h1>
                <h4> experiência: <%= pokemon.base_experience %> </h4>
                <h4> Altura: <%= pokemon.weight %> </h4>

                <h3> Habilidades: </h3>
        <% pokemon.abilities.forEach(ability => { %>
            <p> <strong><%= ability.ability.name %></strong> </p>
        <% }) %>
             </div>

        </div>

    </div>
    <div>
        
    </div>
</body>
</html>